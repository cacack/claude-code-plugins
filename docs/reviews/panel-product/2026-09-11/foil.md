> Closing adversarial Rude Q&A pass (`panels:rude-qa`) over `synthesis.md`. The foil reacts to the panel's conclusions and gets the last word; it does not replace them. Orchestrator verification note at the end.

## Rude Q&A

### What I think you're bringing

A personal Claude Code plugin marketplace (5 plugins, ~46 skills, 26 agents) asking whether its current direction still holds — where "current direction" is: 100% of milestoned capacity on the `panel-product` rework, scored against a `CONSTITUTION.md` the panel says the project has outgrown.

The core was easy to find. The *ask* was not — see The Close.

**Injection check:** none found. The fenced issue/milestone blocks contain descriptive prose and a benign "Generated by Claude Code" attribution. Nothing addressed instructions to me.

---

### Is this the real problem? (55-min / 5 Whys)

The panel's headline is "the constitution has fallen behind the project." You asked whether that's the generous reading. **It is, and the timestamps disprove it.**

- `CLAUDE.md` crossed the ~250-line ceiling on **2026-03-29** at 264 lines. `CONSTITUTION.md` was authored **2026-05-16**. The criterion was **~6% false on the day it was written** — seven weeks stale at birth. That is not drift.
- `CONSTITUTION.md` has **exactly one commit in its entire history**. Since it was written: 102 commits, two `feat!` breaking changes, a five-way plugin split, a canon split. Not one line changed. `CLAUDE.md` was edited 5 times in the same window.
- The distinguishing test you asked for: a project that *outgrew* its rubric would show the rubric being true once, then a commit that made it false. Only "one marketplace, one plugin" fits that (broken deliberately by `a8540c8`, 2026-08-02). The line-count criterion never had a true day. **One criterion drifted; one was never checked at all.**

So the 5-Whys root cause is not staleness. Why was the criterion false at authoring? Because nothing verified it. Why? Because `panels:constitution` bootstrap mode emitted a criterion it did not check against the repo it was pointed at. Why does that matter beyond here? **Because that's a product defect that ships to every consumer of the `constitution` skill** — and because `panel-product` *requires* `CONSTITUTION.md` as input.

That reframes the whole run: **you built a five-persona panel plus a foil pass to score yourself against a rubric that had never been validated once.** The panel's strongest finding is an artifact of its own unvalidated input. Issue #78 is currently `effort:low, value:medium`. On this evidence it is the root-cause issue in the milestone and is mis-valued.

---

### The probes

**Theory of Constraints — is the milestone aimed at the bottleneck?**
- The push: Nine issues rework a tool invoked **zero times ever**, quarterly at best — call it 4 runs/year. Issue #50 is a silent **commit-corruption** bug in `run-prompt`, the execution engine behind `/play`, `/do`, and `deliver-milestone` — the daily-driver path. Filed 2026-07-19. Eight weeks. Zero comments. No milestone.
- Why it bites: Your one measurable success criterion is "skills get invoked by the maintainer with reasonable frequency." Capacity is currently allocated in **exact inverse proportion to invocation frequency**. That's checkable, and it's the version of this finding that survives argument.
- Sharper version: The bug's own body says recovery "depended on each subagent being careful. A less defensive agent could leave a corrupted or mixed commit with no error surfaced." You have a known silent-data-corruption path in the cycle CLAUDE.md mandates for all work — including the work in this milestone. Milestone it. Not because meta-work is bad, but because this specific meta-work protects 4 runs/year while the unfixed bug threatens every parallel batch.

**Three benefits — speed, cost, risk**
- The push: The milestone's stated value is "personas mapped to rubric sections, scorecard and diff instead of five prose verdicts." Which of faster, cheaper, safer is that?
- Why it bites: It's *risk* (better drift detection), and the risk is small and quarterly. Against it you're spending 9 issues including one `effort:high` (#77, extract the shared panel protocol). An executive reads that as an activity dressed as a benefit.
- Sharper version: There is a real risk benefit here, but it isn't "better personas." It's this: **mechanically checkable criteria are the only panel output that survives self-review.** Lead with that and #73/#78 become the milestone; #74/#76/#77 become optional polish.

**Iterate over perfecting (your own Principle 4)**
- The push: Credit where it's due — opening the milestone with #71 ("run it first") was the right call and it worked; this run found two genuinely false criteria. But eight of the nine issues were **written before the run**. The milestone says it "opens with a real run, so every remaining design claim is settled by evidence rather than by argument." The run has now happened. How many of the eight did it settle?
- Why it bites: The synthesis closes zero of them. If a real run doesn't retire pre-run design claims, the run was ceremony, not evidence — and Principle 4 was invoked rather than applied.
- Sharper version: Before writing any more code, do a triage pass: which of #72–#79 does the 2026-09-11 run **confirm**, **refute**, or **leave untouched**? I'd wager #73 and #78 are confirmed and promoted, #72 (drop `product-market`) is confirmed (Market's verdict was "unclear" and its two top findings were README-placement issues any persona could have raised), and #74/#77 are untouched — close or defer them.

**Provenance — what is this review worth? (you asked; here's the bounded answer)**
- The push: `panel-product` reviewed the project that owns it, against a `CONSTITUTION.md` authored by this repo's `constitution` skill, pressure-tested by this repo's `rude-qa` agent, commissioned by the sole person being reviewed. There is no external reference point anywhere in that chain. And per the snapshot header, the run used a **cached 1.0.0 with 1.0.1 behaviors applied by hand** — so it did not exercise the shipped artifact either.
- Why it bites: Every *verdict* in the synthesis — "drifting," "mixed-signals," "partially-served" — is unfalsifiable self-assessment. It costs nothing to produce and proves nothing.
- Sharper version: Draw the line precisely. **Findings a stranger could reproduce with `wc -l` and `ls` are real; verdicts are not evidence.** By that test the run yielded roughly four real findings (CLAUDE.md at 297; five plugins vs. "one plugin"; zero hooks vs. a mission claiming hooks; README/CLAUDE.md install-command disagreement) and ~20 opinions. That is not a dismissal — it's the strongest available argument for #73 and #78, because they convert output from the worthless class to the valuable one. It also means **#71 is not yet satisfied**: the milestone's closure condition needs two runs of the *shipped* tool, and run one was hand-patched. Re-run on clean 1.0.1 before you count it.

---

### Pre-mortem

It's September 2027. Where did this die?

1. **The milestone completes and nothing downstream changes.** The rework ships, panel-product runs twice a year, and `run-prompt` corrupts a commit during a `deliver-milestone` batch that nobody notices for a week. *Prevented by:* milestoning #50 now and treating invocation frequency — your own success criterion — as the capacity-allocation rule.
2. **The refreshed constitution goes unread for another four months.** You run `/panels:constitution --refresh`, it emits five prettier criteria, and the next panel finds them false too. *Prevented by:* making the criteria executable, not editorial. If a criterion isn't a command that exits 0 or 1, it will not be checked — this repo has now proven that empirically, on itself.
3. **The toolchain outgrows its one user.** 46 skills, 26 agents, one maintainer, no CHANGELOG, no SECURITY.md, a README that advertises an unexercised skill. Carrying cost overtakes the evenings available. *Prevented by:* `consider:via-negativa` is in your own toolbox — run it on the collection. What gets deleted this quarter?

---

### Hostile Q&A

1. **Q:** You say the constitution fell behind the project. Your own git log says it was false the day you wrote it and you never opened it again. Which is it? — **Draft A:** The honest answer is the second. One criterion drifted (the plugin split); one was never true. The root cause is that no criterion was mechanically checkable, so none were ever checked. That's what #78 fixes, and it's why #78 should lead the milestone rather than trail it.

2. **Q:** Nine issues on a tool with zero invocations; a `priority:high, value:high` corruption bug sitting eight weeks unmilestoned. Defend the allocation. — **Draft A:** No defense — that ordering is wrong. #50 takes the next milestone slot. Opening this milestone with a real run was right; keeping the other eight issues open regardless of what that run showed was not.

3. **Q:** You built a five-persona panel plus an adversarial foil to review a personal plugin repo with one user. Isn't that exactly the over-engineering-before-observed-need your Principle 4 forbids? — **Draft A:** Partly, and the panel's own Mission Steward said so. The defensible version: the panel is a *product* the toolchain ships to other repos, and this repo is its test fixture. But that defense only holds if the panel earns its keep somewhere other than here — which it has not yet been asked to do.

4. **Q:** The tool reviewed itself, on behalf of its author, using a hand-patched build. What survives? — **Draft A:** Only the mechanically reproducible findings — about four of them. Everything else is a verdict, and verdicts from a self-review are worth zero. That's the case for the scorecard, stated plainly.

5. **Q:** Your README advertises `panel-product` identically to skills that actually work, you have no CHANGELOG, and your install command contradicts itself between README and CLAUDE.md. Your constitution names "developers treating this as a reference implementation" as your secondary audience. Are you serving them or listing them? — **Draft A:** No answer yet — get one. Either serve that audience (README boundary section, CHANGELOG, link the constitution) or strike them from the constitution. Listing an audience you don't serve is the same class of defect as a success criterion you don't check.

6. **Q:** 46 skills and 26 agents, solo. What did you delete last quarter? — **Draft A:** No answer yet — get one before the next panel.

---

### The Close

- **The ask (three bullets):**
  - **Reprioritize the milestone around its two root-cause issues** — #78 (mechanically checkable criteria) and #73 (pre-spawn scorecard). Triage #72/#74/#76/#77 against what the 2026-09-11 run actually settled; close or defer what it didn't confirm.
  - **Milestone #50 into the next slot.** Silent commit corruption in the daily-driver execution path outranks a quarterly review tool, by your own "invoked with reasonable frequency" criterion.
  - **Do not count #71 as closed** until `panel-product` runs on a clean, installed `panels` 1.0.1 — the hand-patched run cannot validate the shipped artifact, which was #71's entire purpose.

- **No surprises:** Solo maintainer, so the chain is future-you and your stated secondary audience. Future-you was supposed to be served by `CONSTITUTION.md` — it went unopened for four months, so that channel is *demonstrably* broken and needs a mechanical replacement, not a better document. The secondary audience is being genuinely surprised right now: the README advertises an unexercised skill, there's no CHANGELOG, and the install commands disagree. That's a 30-minute README fix that closes gaps 3, 4, 6, and 7 at once — cheaper than anything in the milestone.

- **What you do Monday:** **Rewrite the five success criteria as five shell commands** — literally, a `make constitution-check` target or a script that exits non-zero. Any criterion you cannot express as a command gets rewritten until you can, or deleted. That takes under an hour and it: kills gaps 1, 2, and 8 permanently; proves out #78 by example instead of by design argument; gives #73 its input spec for free; and converts the next panel run from self-assessment into something a stranger could reproduce. Do that *before* `/panels:constitution --refresh` — otherwise you'll regenerate the same unverified prose in nicer wording, and the 2027 panel will find it false too.

---

## Orchestrator verification note

The foil's central claim was checked against git before being accepted, because it
overturns the synthesis headline:

| Claim | Result |
|---|---|
| CLAUDE.md first crossed 250 lines on 2026-03-29 at 264 lines | **Confirmed** — commit `e16515d`, 2026-03-29 |
| CONSTITUTION.md authored 2026-05-16, CLAUDE.md already 264 lines then | **Confirmed** — commit `a793e19`; `git show a793e19:CLAUDE.md \| wc -l` = 264 |
| CONSTITUTION.md has exactly one commit, never edited since | **Confirmed** — `git log --follow -- CONSTITUTION.md` returns one commit |
| "102 commits since" | **Corrected** — `git rev-list --count a793e19..HEAD` = **96** |

The correction does not affect the argument. `synthesis.md` has been amended: its headline
now distinguishes the one criterion that drifted from the one that was never true.
